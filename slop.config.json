{
  "$comment": "slopgate measuring itself \u2014 see the `self` CI job. Ceilings stay null: this repo is small enough that a single new branch swings the ratio hard, so the number is reported for dogfooding rather than gated on.",
  "languages": {
    "typescript": {
      "include": [
        "src/**/*.ts",
        "bin/**/*.ts"
      ],
      "exclude": [
        "**/node_modules/**"
      ]
    }
  },
  "maxDelta": {
    "erosion": 0.0053,
    "verbosity": 0.0022
  },
  "thresholds": {
    "erosion": null,
    "verbosity": null
  },
  "calibratedAtRulePackVersion": 0
}